# 多 Agent 协作规范

当多个 AI Agent 共同在此仓库中执行任务时，必须遵守以下协作约束：

1. **精确分工**：
   - **Antigravity**：主要负责 UI 微调、视觉优化、组件样式调整。
   - **Claude Code (或其他主逻辑 Agent)**：主要负责架构搭建、核心组件逻辑编写、修复 Bug。

2. **操作限制**：
   - 每个 Agent 在接到任务时，只修改任务指定的文件。
   - 严禁互相覆盖代码或随意重构彼此负责的领域。
   - 绝不允许同时让多个 Agent 修改同一个文件，以避免代码冲突。

3. **安全规范**：
   - 不要读取密钥、.env、以及任何系统/本地私密文件。
   - 不要引入破坏性构建变更。

4. **沟通与记录**：
   - 修改前列出需要修改的文件列表。
   - 修改后仅总结发生变更的内容（Changed / Added）。
